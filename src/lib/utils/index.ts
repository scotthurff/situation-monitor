/**
 * Utils barrel export
 */

export {
	formatRelativeTime,
	formatCompactNumber,
	formatPercent,
	formatCurrency,
	truncate,
	stripHtml,
	capitalize,
	generateId
} from './format';

export { viewport } from './viewport.svelte';
